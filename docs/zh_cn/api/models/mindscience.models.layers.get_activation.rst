mindscience.models.layers.get_activation
===================================================

.. py:function:: mindscience.models.layers.get_activation(name)

    获取激活函数。

    参数：
        - **name** (Union[str, None]) - 激活函数的名称。如果 `name` 为 ``None``，则返回 ``None``。

    返回：
        Function(mindspore.cell)，激活函数。


mindelec.loss.Constraints
=========================

.. py:class:: mindelec.loss.Constraints(dataset, pde_dict)

    所有子数据集的损失定义。

    参数：
        - **dataset** (Dataset) - 包括pde方程、边界条件和初始条件的数据集。
        - **pde_dict** (dict) - pde问题的dict。

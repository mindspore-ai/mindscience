mindchemistry.utils.load_yaml_config
=========================================

.. py:function:: mindchemistry.utils.load_yaml_config(file_path)

    加载一个 YAML 配置文件。

    参数：
        - **file_path** (str) - YAML 配置文件的路径。

    返回：
        - **config** (dict) - 以字典格式返回的配置信息。


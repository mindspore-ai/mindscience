sciai.utils.set_seed
=======================

.. py:function:: sciai.utils.set_seed(seed=1234)

    设置各种地方的随机种子。

    参数：
        - **seed** (int) - 随机种子数字。

    异常：
        - **TypeError** 如果 `seed` 不是整数。
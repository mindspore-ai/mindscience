sciai.utils.datatype2np
==============================================

.. py:function:: sciai.utils.datatype2np(ms_type)

    从 `MindSpore` 数据类型到 `numpy` 数据类型的映射。

    参数：
        - **ms_type** (dtype) - `MindSpore` 数据类型。

    返回：
        - **numpy.dtype** - `numpy` 数据类型。
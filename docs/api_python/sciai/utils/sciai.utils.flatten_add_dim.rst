sciai.utils.flatten_add_dim
============================

.. py:function:: sciai.utils.flatten_add_dim(*data)

    展平数据并在末尾增加额外一个维度。

    参数：
        - **\*data** (np.array) - 用于展平与升维的数据。
    返回：
        tuple[np.array]，转化后的数据
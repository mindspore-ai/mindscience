sciai.architecture.get_activation
=====================================

.. py:function:: sciai.architecture.get_activation(activation)

    根据名字获取对应激活函数。

    参数：
        - **activation** (str) - 激活函数的名字。

    返回：
        Callable，对应的激活函数。
sciai.architecture.Swish
=========================

.. py:class:: sciai.architecture.Swish()

    带反向传播的 Swish(Silu) 激活函数。

    输入：
        - **x** (Tensor) - Swish(Silu)的输入。

    输出：
        Tensor，shape与 `x` 一致的被激活的输出。
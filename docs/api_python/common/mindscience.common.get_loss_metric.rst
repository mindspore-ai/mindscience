mindscience.common.get_loss_metric
==================================

.. py:function:: mindscience.common.get_loss_metric(name)

    获取对应名称的损失函数实例。

    参数：
        - **name** (str) - 损失函数的名称。

    返回：
        Function，损失函数。

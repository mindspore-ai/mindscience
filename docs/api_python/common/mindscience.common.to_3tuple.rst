mindscience.common.to_3tuple
============================

.. py:function:: mindscience.common.to_3tuple(t)

    将整数或整数元组转换为 3 元组。

    参数：
        - **t** - 整数或整数元组，用于表示网格的高度和宽度。

    返回：
        与输入相同，或者是一个 `(t, t, t)` 形式的元组。
mindsponge.data.write_yaml
==========================

.. py:function:: mindsponge.data.write_yaml(filename, data)

    写入yaml文件。

    参数：
        - **filename** (str) - yaml文件的名称。
        - **data** (dict) - 需要写入yaml文件的数据。
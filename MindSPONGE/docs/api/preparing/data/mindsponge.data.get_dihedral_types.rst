mindsponge.data.get_dihedral_types
==================================

.. py:function:: mindsponge.data.get_dihedral_types(atom_types, symbol="-")

    多原子名称构造函数。

    参数：
        - **atom_types** (ndarray) - 原子种类。
        - **symbol** (str) - 一个符号。默认值："-"。

    输出：
        - ndarray。种类。
        - ndarray。逆类型。
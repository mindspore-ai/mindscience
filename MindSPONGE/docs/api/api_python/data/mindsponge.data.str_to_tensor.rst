mindsponge.data.str_to_tensor
=============================

.. py:class:: mindsponge.data.str_to_tensor(string)

    把字符串编码为Tensor。

    参数：
        - **string** (str) - 输入的字符串。

    输出：
        Tensor。转换后的Tensor。
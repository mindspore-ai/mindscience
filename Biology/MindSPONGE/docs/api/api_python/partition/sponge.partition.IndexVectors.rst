sponge.partition.IndexVectors
==============================

.. py:class:: sponge.partition.IndexVectors(use_pbc: bool = None)
    
    按索引获取向量。

    参数：
        - **use_pbc** (bool，可选) - 是否使用周期性边界条件。默认值： ``None``，默认取 ``False``。。

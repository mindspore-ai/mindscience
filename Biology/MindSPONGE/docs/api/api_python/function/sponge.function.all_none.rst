sponge.function.all_none
============================

.. py:function:: sponge.function.all_none(iterable: Iterable)

    如果 `iterable` 中的 ALL 值 `x` 为 None，返回True。

    参数：
        - **iterable** (Iterable) - 可迭代变量。

    返回：
        bool。all。如果 `iterable` 中的所有值 `x` 为 None。

